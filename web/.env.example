# Локальная база. При переезде на Postgres: postgresql://user:pass@host:5432/db
DATABASE_URL="file:./dev.db"

# Секрет для подписи сессионных кук. В продакшене обязательно свой:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=""

# Учётка администратора, создаётся при первом сиде
ADMIN_EMAIL="admin@thecapital.am"
ADMIN_PASSWORD=""
